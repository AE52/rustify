# openapi-typescript 7.x still declares a `typescript@^5` peer range, but the
# project builds on typescript 6. The mismatch is a peer-range lag, not a real
# runtime incompatibility, so allow the install to proceed.
legacy-peer-deps=true
